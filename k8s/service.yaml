apiVersion: v1
kind: Service
metadata:
  name: mcp-demo-server
  namespace: mcp-servers
spec:
  selector:
    app: mcp-demo-server
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP